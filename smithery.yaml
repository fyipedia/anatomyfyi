# Smithery configuration for anatomyfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'anatomyfyi[mcp]', 'python', '-m', 'anatomyfyi.mcp_server'] })
